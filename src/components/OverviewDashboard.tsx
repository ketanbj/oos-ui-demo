export const OverviewDashboard = () => {
  return (
    <section id="overview" className="space-y-4">
      <div className="glass rounded-2xl p-4">
        <div className="text-sm text-slate-300">
          The core stats and filters now live in the pinned header. Use the layer toggles in the header to adjust the orbital view overlays.
        </div>
      </div>
    </section>
  )
}
